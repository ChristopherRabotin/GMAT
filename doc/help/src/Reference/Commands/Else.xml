<?xml version="1.0" encoding="UTF-8"?>
<section version="5.0" xmlns="http://docbook.org/ns/docbook"
xmlns:xlink="http://www.w3.org/1999/xlink"
xmlns:xi="http://www.w3.org/2001/XInclude"
xmlns:svg="http://www.w3.org/2000/svg"
xmlns:m="http://www.w3.org/1998/Math/MathML"
xmlns:html="http://www.w3.org/1999/xhtml"
xmlns:db="http://docbook.org/ns/docbook">

<title>Else</title>
<para>This was created using the June 9, 2008 build of GMAT.</para>
<section>
<title>Overview</title>
<para>If-Else statements in GMAT work as they do in other programming languages, especially Matlab.  The Else command adds another dimension to an If statement.  You use an Else statement when you want something to happen when the conditions of an If statement are not met.  For example, an If statement who's condition is "-+x ~060~ 5+-" will only execute the script within its scope when -+x+- is indeed less than 5.  GMAT would otherwise pass over the If statement's associated script and continue.  However, having an Else statement after the If will ensure that the lines of script within the scope of that Else are executed when -+x+- is equal to 5 or greater.</para>
<section>
<title>Listing of Objects and Commands that interact</title>
<itemizedlist>
<listitem><para>[tiki-index.php?page=If+Command|If]</para>
<itemizedlist>
<listitem><para>The If command is the only part of GMAT that interacts with the Else command.  Whenever Else is used, If must precede it as described below.</para></listitem>
</itemizedlist></listitem>
</itemizedlist>
</section>
</section>
<section>
<title>Using the Else Command in the GUI</title>
<para>Else appears as the option 'If-Else' in the GUI when adding it to the Mission tree.  Once added to the tree, there will appear an If with an Else appended to it.  You may add any command(s) you wish after the Else statement.</para>
<para>{img src=show_image.php?name=ElseMain.jpg align="center"}</para>
<para>
<screenshot><info><title></title></info><mediaobject><imageobject><imagedata align="center"
fileref="???"></imagedata></imageobject>
<caption><para></para></caption></mediaobject></screenshot></para>
</section>
<section>
<title>Using the Else Command in the script</title>
<section>
<title>Script Syntax</title>
<para>^</para>
<para>-+If ~060~logical expression~062~;+-</para>
<para>-+~hs~~hs~~060~Statements~062~;+-</para>
<para>-+Else;+-</para>
<para>-+~hs~~hs~~060~Statements~062~;+-</para>
<para>-+EndIf;+-</para>
<para>^</para>
</section>
<section>
<title>Script Examples</title>
<para>^</para>
<para>-+If DefaultSC.ElapsedDays ~060~ 1;+-</para>
<para>-+~hs~~hs~Propagate DefaultProp( DefaultSC , { DefaultSC.ElapsedDays = 0.01 });+-</para>
<para>-+Else;+-</para>
<para>-+~hs~~hs~Propagate DefaultProp( DefaultSC , { DefaultSC.ElapsedDays = 0.2 });+-</para>
<para>-+EndIf;+-</para>
<para>^</para>
</section>
</section>
</section>